import { Button } from '@/components/ui/button'

export const AddLocationParkCarButton = () => {
    const handlePress = () => {}

    return (
        <Button onPress={handlePress} variant="minor">
            Park my car
        </Button>
    )
}
